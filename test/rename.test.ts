require('expect.js');

import * as vscode from 'vscode';
import { FileItem } from '../src/extension/api/item';
import { FileController } from '../src/extension/api/controller';

suite('Api Controller Tests', () => {

    suite('testing rename method', () => {
        
    });
});